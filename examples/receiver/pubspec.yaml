name: receiver

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  logging: '^1.2.0'
  screen_streamer:
    path: ../../

dev_dependencies:
  flutter_lints: '^3.0.1'
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
